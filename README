# Fractal plotting for a paper about fractals and economics

To run this analysis, you'll need Numpy and Matplotlib.

Run 
    bin/calc_fractal.py 3 cubic.dat
to generate the cubic data, then run `fig1.py`.

Run 
    bin/calc_fractal.py 5 quintic.dat
to generate the quintic data, then run `fig2.py`.

Run 
    bin/calc_fractal.py 6 sextic.dat
to generate the sextic data, then run `fig3.py`.

Run
    jupyter nbconvert --to notebook --execute bin/maizeexports.ipynb
to generate plots of maize exports by country and farm subsidies by
crop area in the USA.

Replace `secret_key.dat` with your own secret key to be able to run
on your cloud account.
